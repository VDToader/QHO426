def listen():           #First, we have to declare the user function
    sound = input ("Please enter a word representing a sound: ")    #User's response will be stored inside sound variable
    print(f"Thaw was a loud {sound}")
listen()        #On the last steep we need to call the function